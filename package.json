{
  "name": "tesco-monitor",
  "version": "2.0.0",
  "description": "Background multi-endpoint probe service with on-demand dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
